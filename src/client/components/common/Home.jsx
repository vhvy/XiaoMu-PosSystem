import React from "react";
import { Layout, } from "antd";

const { Header, Content, Footer } = Layout;

function Home() {
    return (
        <Layout>
            <Header></Header>
            <Content></Content>
        </Layout>
    );
}

export default Home;